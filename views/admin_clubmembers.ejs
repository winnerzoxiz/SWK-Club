<!DOCTYPE html>
<html lang="th">
<head>
  <!-- -------------------- Meta & Title -------------------- -->
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin - รายชื่อสมาชิกชุมนุม</title>

  <!-- -------------------- CSS & Icon -------------------- -->
  <link rel="stylesheet" href="/style-clubmembers.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
  <div class="main p-3">
    <!-- -------------------- Header -------------------- -->
    <div class="main-header d-flex align-items-center justify-content-between mb-3">
      <div>
        <!-- ปุ่มย้อนกลับไปหน้าชุมนุมที่ยังว่าง -->
        <a href="/admin_club_capacity">
          <button class="btn btn-success ms-1"><i class="bi bi-arrow-left"></i></button>
        </a>
      </div>
      <!-- ชื่อชุมนุมที่แสดงอยู่ -->
      <h1 class="header_title fw-bold m-0 mx-auto text-center">
        รายชื่อนักเรียนในชุมนุม <%= clubName %>
      </h1>
      <!-- ปุ่ม Export Excel รายชื่อสมาชิก -->
      <a href="/admin_clubmembers/export/:classroom" class="btn btn-success">
        📥 Export Excel
      </a>
    </div>

    <!-- -------------------- Member Table -------------------- -->
    <table class="table table-bordered table-striped">
      <thead class="table-dark">
        <tr>
          <th class="text-center">ชื่อนักเรียน</th>
          <th class="text-center">ชั้น</th>
          <th class="text-center">เลขที่</th>
          <th class="text-center">อีเมล</th>
          <th class="text-center">กลุ่มสาระการเรียนรู้</th>
          <th class="text-center">ครูที่ปรึกษา</th>
        </tr>
      </thead>
      <tbody>
        <% if (members.length > 0) { %>
          <% members.forEach(m => { %>
            <tr>
              <td class="text-center"><%= m.name_users %></td>
              <td class="text-center"><%= m.classroom %></td>
              <td class="text-center"><%= m.number %></td>
              <td class="text-center"><%= m.email %></td>
              <td class="text-center"><%= m.category %></td>
              <td class="text-center"><%= m.name_teachers %></td>
            </tr>
          <% }) %>
        <% } else { %>
          <!-- กรณีไม่มีสมาชิกในชุมนุมนี้ -->
          <tr>
            <td colspan="6" class="text-center">ยังไม่มีนักเรียนในชุมนุมนี้</td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>
